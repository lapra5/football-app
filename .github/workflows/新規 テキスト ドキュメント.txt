cd F:\Python\practice\web_sample\my-app

# すべての変更をステージに追加
git add -A

# コミット（メッセージは適宜変更してください）
git commit -m "🔥 Update match range script to use Timestamp and fetch 30-day window"

# GitHub にプッシュ
git push origin master
