cd F:\Python\practice\web_sample\my-app

# 変更ファイルをすべてステージング
git add .

# コミット
git commit -m "🔁 自動化スクリプト＆Actionsの通知対応 を全体に適用"


git pull origin master --rebase

# プッシュ
git push origin master
